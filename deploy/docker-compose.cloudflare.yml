services:
  cloudflared:
    image: cloudflare/cloudflared:2026.2.0
    container_name: elderly-companion-cloudflared
    command: tunnel --no-autoupdate run --token ${CLOUDFLARED_TOKEN}
    restart: unless-stopped
    depends_on:
      - gateway

